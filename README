# Node.js TypeScript Project

A basic Node.js project written in TypeScript.

## Prerequisites
- Node.js 20.x
- Docker (Optional)

## Running the Project

### Without Docker
1. Install dependencies: `npm install`
2. Start the server: `npm start`
3. Build the project: `npm run build`
4. Run the linter: `npm run lint`

### With Docker
Ensure Docker is installed and running, then:

1. Build the Docker image: `docker build -t my-node-ts-project .`
2. Run the Docker image: `docker run -p 3000:3000 my-node-ts-project`

Now, the server is accessible at `http://localhost:3000`.
